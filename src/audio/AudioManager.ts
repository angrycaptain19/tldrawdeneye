/**
 * AudioManager
 *
 * Web Audio API wrapper providing positional sound effects and ambient music.
 * Must be initialised on a user gesture to satisfy browser autoplay policies.
 *
 * TODO: implement sound loading, positional attenuation, and music scheduling.
 */
export class AudioManager {
  // placeholder
}

/** Singleton instance used throughout the game. */
export const audioManager = new AudioManager()
