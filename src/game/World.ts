/**
 * World
 *
 * Holds the tile grid (2-D number array) that defines walls, floors, doors,
 * and spawn points for a level.  Also owns the texture / colour palette
 * looked up by the raycaster.
 *
 * TODO: implement level loading and tile query helpers.
 */
export class World {
  // placeholder
}
