/**
 * EnemySystem
 *
 * Spawns and updates all enemy agents.  Each enemy has a simple state machine
 * (patrol → alert → attack → dead) and a line-of-sight check against the
 * world grid.
 *
 * TODO: implement enemy AI.
 */
export class EnemySystem {
  // placeholder
}
