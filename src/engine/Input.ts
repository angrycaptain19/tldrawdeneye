/**
 * Input
 *
 * Keyboard / mouse input handler.  Listens for keydown / keyup / mousemove
 * events and exposes a flat state object so game systems can poll it each tick.
 *
 * TODO: implement event listeners and pointer-lock integration.
 */
export class Input {
  // placeholder
}
