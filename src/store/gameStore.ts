/**
 * gameStore
 *
 * Shared reactive state bridging the game engine and the React / tldraw UI.
 * Uses a simple pub/sub or external store pattern so React components can
 * subscribe without pulling the full game loop into React state.
 *
 * TODO: implement store with player stats, enemy positions, door states, etc.
 */
export const gameStore = {
  // placeholder
}
